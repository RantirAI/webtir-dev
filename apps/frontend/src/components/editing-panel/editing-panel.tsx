import { JsonState } from '../dev-only/json-state/json-state';
export const EditingPanelView = () => {
    return (
        <div class="w-[422px] bg-white h-full">
            <JsonState />
        </div>
    )
}